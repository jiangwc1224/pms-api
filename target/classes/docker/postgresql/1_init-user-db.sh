#!/bin/bash
set -e

psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB" <<-EOSQL
    CREATE USER "$PMS_DB_USER" WITH PASSWORD '$PMS_DB_PASSWORD';
    CREATE DATABASE "$PMS_DB_DATABASE" OWNER "$PMS_DB_USER";
EOSQL